{
  "name": "opendray-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Documentation site for opendray — multi-CLI control gateway",
  "scripts": {
    "stub:images": "node scripts/stub-missing-images.mjs",
    "llms": "node scripts/generate-llms-txt.mjs",
    "prebuild": "pnpm stub:images && pnpm llms",
    "predev": "pnpm stub:images && pnpm llms",
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.5.13"
  },
  "packageManager": "pnpm@9.15.0"
}
